import React from "react";

const Pagination = () => {
  return (
    <div className="pagination">
      <button>BACK</button>
      <button>NEXT</button>
    </div>
  );
};

export default Pagination;
