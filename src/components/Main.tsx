import Cards from "./Cards";
import Pagination from "./Pagination";

const Main = () => {
  return (
    <section className="main">
      <Pagination></Pagination>
      <Cards></Cards>
    </section>
  );
};

export default Main;
